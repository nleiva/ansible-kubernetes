aws_region: "{{ region | default('us-east-1') }}"
ec2_resource_prefix: "{{ resource_prefix | default('kubernetes') }}"
ec2_environment: "{{ ec2_resource_prefix }}"
s3_identifier: "{{ aws_s3 | default('store') }}"